import com.tz.test.*; //导包
class CircleTest{
    public static void main(String[] args){

        //实例化一个Circle1对象，调用半径的set()方法给半径赋值，最后调用show()方法，输出结果
        Circle c =new Circle();
        c.setRadius(2);
        c.show();
    }
}